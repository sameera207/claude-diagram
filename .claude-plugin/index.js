'use strict'

module.exports = {
  async initMain(api) {
    // claude-diagram is primarily a Claude Code hooks/commands plugin.
    // Draftflow entry point reserved for future UI contributions.
  },
}
